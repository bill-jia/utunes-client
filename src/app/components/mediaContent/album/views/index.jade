md-content(layout-padding="" style="")
	md-content(layout-padding="" flex)
		div(layout="row")
			h1.md-primary.album-title Albums
			span(flex)
			md-button.md-raised.md-accent(ui-sref="root.albums.new" ng-if = "user.role && (user.role == 'admin' || user.role == 'producer')") Add album

	md-content(layout-padding="")
		grid-list(items="albums" type="'album'" count="count" ng-show="albums")
